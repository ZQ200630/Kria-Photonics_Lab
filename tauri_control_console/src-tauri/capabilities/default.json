{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Main window permissions for progress events.",
  "windows": ["main"],
  "permissions": ["core:event:default"]
}
