{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Main window permission for PA image build progress events.",
  "windows": ["main"],
  "permissions": ["core:event:default"]
}
